Translate the following complete MDX article from English into natural $language_name.

Target locale: $language_name ($lang_code)

Rules:

1. Translate all reader-visible text, including metadata title and description, headings, tables, lists, captions, FAQ questions, and answers.
2. Keep category, date, lastModified, image paths, component names, JSX attributes, URLs, and the English ASCII filename slug unchanged.
3. Preserve valid JavaScript metadata export and MDX syntax. Do not use YAML frontmatter or add a Markdown H1.
4. Keep every source URL unchanged. Do not add, remove, or invent sources.
5. Do not leave English interface phrases or paragraphs unless they are official proper names, platform feature names, item names, or codes that should not be translated.
6. Keep the translated metadata title at no more than 60 characters where natural. Keep description between 140 and 160 characters where the language allows it without losing accuracy.
7. Output only the translated MDX beginning with `export const metadata = {`. Do not wrap it in a code fence and do not explain the translation.

English source article:

$content
